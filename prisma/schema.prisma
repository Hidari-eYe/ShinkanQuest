generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model ClearRecord {
  id        String   @id @default(cuid())
  rank      Int      @unique
  timestamp DateTime @default(now())
}
